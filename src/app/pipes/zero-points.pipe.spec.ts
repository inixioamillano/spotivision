import { ZeroPointsPipe } from './zero-points.pipe';

describe('ZeroPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
